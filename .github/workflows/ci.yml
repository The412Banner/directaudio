name: build driver (arm64ec hot-swap artifacts)

# Builds ONLY dlls/winedirectaudio.drv against a pinned proton-wine tree,
# reusing that repo's exact bionic/arm64ec toolchain (NDK r27d + bylaws
# llvm-mingw). Produces the hot-swap pair the device Proton layer wants:
#   winedirectaudio.drv  (arm64ec PE)   +   winedirectaudio.so  (unixlib)
# for BOTH Wine ABIs (Wine 11.0 and Wine 10.0) and BOTH page sizes
# (4KB/SDK28, 16KB/SDK35) — a 2x2 matrix, 4 artifacts total. The ABI is
# entirely determined by which proton-wine ref each matrix entry pins:
#   * Wine 11  -> ge-proton11-5-bionic-staging (winedirectaudio.drv is a real
#     in-tree dir; overlay directaudio.c + Makefile.in from this repo).
#   * Wine 10  -> proton_10.0 (winedirectaudio.drv is a SUBMODULE pinned to the
#     exact Wine-10 PE side P10-4 shipped: mmdevdrv.c + winedirectaudio.drv.spec
#     + a Makefile.in listing both sources. `submodules: recursive` populates it;
#     we overlay ONLY the shared unix implementation directaudio.c and keep the
#     pinned PE side/Makefile so the artifact is ABI-identical to P10-4).
# On a directaudio-v* tag all 4 attach to a GitHub release. On-device audio is
# verified separately.

on:
  push:
    branches: [ main ]
    tags: [ 'directaudio-v*' ]
  pull_request:
  workflow_dispatch:

env:
  PROTON_REPO: The412Banner/proton-wine

jobs:
  build:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        include:
          # Wine 11.0 ABI (ge-proton11-5-bionic-staging)
          - label: wine11-arm64ec-sdk28    # 4KB-page devices
            abi: wine11
            proton_ref: ge-proton11-5-bionic-staging
            page_flag: ""
          - label: wine11-arm64ec-sdk35    # 16KB-page devices
            abi: wine11
            proton_ref: ge-proton11-5-bionic-staging
            page_flag: "--enable-16kb-pages"
          # Wine 10.0 ABI (proton_10.0 — the tree P10-4 shipped from)
          - label: wine10-arm64ec-sdk28    # 4KB-page devices
            abi: wine10
            proton_ref: proton_10.0
            page_flag: ""
          - label: wine10-arm64ec-sdk35    # 16KB-page devices
            abi: wine10
            proton_ref: proton_10.0
            page_flag: "--enable-16kb-pages"
    steps:
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/share/boost "$AGENT_TOOLSDIRECTORY"
          df -h | head -2

      - name: Checkout pinned Wine tree (proton-wine)
        uses: actions/checkout@v4
        with:
          repository: ${{ env.PROTON_REPO }}
          ref: ${{ matrix.proton_ref }}
          path: wine
          submodules: recursive   # Wine-10: populates the winedirectaudio.drv submodule (PE side)

      - name: Checkout this driver (source under test)
        uses: actions/checkout@v4
        with:
          path: driver

      - name: Overlay driver source into the Wine tree
        run: |
          set -e
          if [ "${{ matrix.abi }}" = "wine11" ]; then
            # Wine 11: mmdevapi loads the driver via the unixlib (no PE-side GUID
            # exports). Overlay the shared unix implementation + its Makefile.
            cp -v driver/directaudio.c driver/Makefile.in wine/dlls/winedirectaudio.drv/
          else
            # Wine 10: the pinned proton_10.0 submodule already carries the exact
            # Wine-10 PE side P10-4 shipped (mmdevdrv.c + winedirectaudio.drv.spec +
            # a Makefile.in listing both sources). Trust it to avoid ABI drift;
            # overlay ONLY the shared unix implementation under test.
            test -f wine/dlls/winedirectaudio.drv/mmdevdrv.c            # submodule populated?
            test -f wine/dlls/winedirectaudio.drv/winedirectaudio.drv.spec
            grep -q 'mmdevdrv\.c' wine/dlls/winedirectaudio.drv/Makefile.in  # PE side wired in
            cp -v driver/directaudio.c wine/dlls/winedirectaudio.drv/
          fi
          echo "--- winedirectaudio.drv sources for ${{ matrix.label }} ---"
          ls -la wine/dlls/winedirectaudio.drv/
          # Integration deltas (configure.ac + mmdevapi default_list) already live in
          # the pinned proton-wine ref; nothing to patch here.

      - name: Set up build environment
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y \
            build-essential git wget curl unzip flex bison gettext \
            autoconf automake libtool pkg-config mingw-w64 \
            gcc-multilib g++-multilib ccache \
            libfreetype6-dev libfreetype6-dev:i386 \
            libpng-dev libpng-dev:i386 zlib1g-dev zlib1g-dev:i386

      - name: Download termuxfs (aarch64)
        run: |
          mkdir -p $HOME/termuxfs/aarch64 && cd $HOME/termuxfs/aarch64
          wget -q https://github.com/GameNative/termux-on-gha/releases/download/build-20260218/termuxfs-aarch64.tar
          tar -xf termuxfs-aarch64.tar

      - name: Cache Android NDK
        id: cache-ndk
        uses: actions/cache@v4
        with:
          path: ~/Android/Sdk/ndk/27.3.13750724
          key: android-ndk-r27d
      - name: Set up Android NDK
        if: steps.cache-ndk.outputs.cache-hit != 'true'
        run: |
          mkdir -p $HOME/Android/Sdk/ndk && cd $HOME/Android/Sdk/ndk
          wget -q https://dl.google.com/android/repository/android-ndk-r27d-linux.zip
          unzip -q android-ndk-r27d-linux.zip
          mv android-ndk-r27d 27.3.13750724

      - name: Cache LLVM MinGW toolchain
        id: cache-llvm-mingw
        uses: actions/cache@v4
        with:
          path: ~/toolchains/llvm-mingw-20250920-ucrt-ubuntu-22.04-x86_64
          key: bylaws-llvm-mingw-20250920
      - name: Set up LLVM MinGW toolchain
        if: steps.cache-llvm-mingw.outputs.cache-hit != 'true'
        run: |
          mkdir -p $HOME/toolchains && cd $HOME/toolchains
          wget -q https://github.com/bylaws/llvm-mingw/releases/download/20250920/llvm-mingw-20250920-ucrt-ubuntu-22.04-x86_64.tar.xz
          tar -xf llvm-mingw-20250920-ucrt-ubuntu-22.04-x86_64.tar.xz

      - name: Autogen
        working-directory: wine
        run: |
          if [ "${{ matrix.abi }}" = "wine11" ]; then
            # Wine 11 wrapper: applies server_protocol.def.patch, then ./autogen.sh
            bash build-scripts/build-step0-autogen.sh
          else
            # Wine 10 (proton_10.0) has no build-step0-autogen.sh; its own build
            # workflows autogen via ./autogen.sh --aarch64 — mirror that exactly.
            bash autogen.sh --aarch64
          fi

      - name: Cache wine-tools
        id: cache-wine-tools
        uses: actions/cache@v4
        with:
          path: wine/wine-tools
          key: wine-tools-${{ matrix.proton_ref }}-${{ hashFiles('wine/configure.ac') }}
      - name: Build wine-tools
        if: steps.cache-wine-tools.outputs.cache-hit != 'true'
        working-directory: wine
        run: bash build-scripts/build-step0.sh

      - name: Cache ccache (${{ matrix.label }})
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ccache-da-${{ matrix.label }}-${{ github.sha }}
          restore-keys: |
            ccache-da-${{ matrix.label }}-

      - name: Build sysvshm (satisfies configure's --with-xshm requirement)
        working-directory: wine
        run: bash build-scripts/build-step-arm64ec.sh --build-sysvshm

      - name: Configure + build ONLY the DirectAudio driver
        working-directory: wine
        shell: bash
        run: |
          set -eo pipefail
          # `source` so the arm64ec toolchain env the script exports (CC/CFLAGS/
          # CROSSCFLAGS/PATH/DLLTOOL/…) persists into this shell for the targeted
          # make below. The script also applies the bionic + GE patches + configures.
          source build-scripts/build-step-arm64ec.sh ${{ matrix.page_flag }} --configure
          # Discover the exact build targets from the generated Makefile. PE files
          # live in a per-arch subdir ({aarch64,i386}-windows/); the unixlib .so sits
          # at the module-dir root. Match ONLY real targets: require the "-windows/"
          # arch segment for the .drv, or the bare .so. This deliberately EXCLUDES the
          # bare "dlls/winedirectaudio.drv/winedirectaudio.drv" string that Wine 10's
          # Makefile carries (the PE .spec/importlib reference) — it is not a make
          # target, and feeding it to make fails with "No rule to make target". The
          # Wine 11 tree has no such bare reference, so its target set is unchanged.
          mapfile -t TGTS < <(grep -oE "dlls/winedirectaudio\.drv/([A-Za-z0-9_-]+-windows/winedirectaudio\.drv|winedirectaudio\.so)" Makefile | sort -u)
          echo "discovered targets:"; printf '  %s\n' "${TGTS[@]}"
          test "${#TGTS[@]}" -ge 2   # expect at least the PE + the unixlib
          echo "::group::make driver"
          make -j"$(nproc)" "${TGTS[@]}"
          echo "::endgroup::"
          echo "=== driver build products ==="
          find dlls/winedirectaudio.drv -name 'winedirectaudio.*' ! -name '*.c' -exec ls -la {} +

      - name: Collect + strip artifacts (complete 3-file set)
        working-directory: wine
        run: |
          set -e
          STRIP="$HOME/Android/Sdk/ndk/27.3.13750724/toolchains/llvm/prebuilt/linux-x86_64/bin/llvm-strip"
          OUT="$GITHUB_WORKSPACE/out/${{ matrix.label }}"
          mkdir -p "$OUT/aarch64-windows" "$OUT/i386-windows" "$OUT/aarch64-unix"
          # A COMPLETE driver is THREE files, laid out to mirror the Proton layer so
          # the whole set can be dropped straight onto lib/wine/:
          #   aarch64-windows/winedirectaudio.drv  arm64ec PE — loaded by 64-bit guests
          #   i386-windows/winedirectaudio.drv     32-bit PE  — loaded by 32-bit guests
          #   aarch64-unix/winedirectaudio.so      bionic unixlib — the AAudio backend
          # Both PE arches come from the one driver source; the build produced both.
          PE64=$(find . -type f -path '*/aarch64-windows/winedirectaudio.drv' | head -1)
          PE32=$(find . -type f -path '*/i386-windows/winedirectaudio.drv'    | head -1)
          SO=$(find . -type f -name 'winedirectaudio.so' | head -1)
          echo "PE64=$PE64"; echo "PE32=$PE32"; echo "SO=$SO"
          test -n "$PE64" && test -n "$PE32" && test -n "$SO"   # all three required
          cp -v "$PE64" "$OUT/aarch64-windows/winedirectaudio.drv"
          cp -v "$PE32" "$OUT/i386-windows/winedirectaudio.drv"
          cp -v "$SO"   "$OUT/aarch64-unix/winedirectaudio.so"
          "$STRIP" --strip-all "$OUT/aarch64-windows/winedirectaudio.drv" 2>/dev/null || true
          "$STRIP" --strip-all "$OUT/i386-windows/winedirectaudio.drv"    2>/dev/null || true
          "$STRIP" --strip-all "$OUT/aarch64-unix/winedirectaudio.so"     2>/dev/null || true
          echo "--- complete artifact set (${{ matrix.label }}) ---"
          find "$OUT" -type f -exec ls -la {} +
          echo "--- file(1) identification ---"
          find "$OUT" -type f -exec file {} +

      - name: Upload hot-swap artifacts
        uses: actions/upload-artifact@v4
        with:
          name: directaudio-${{ matrix.label }}
          path: out/${{ matrix.label }}
          retention-days: 30

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/directaudio-v')
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Stage per-page-size zips
        run: |
          set -e
          mkdir -p release
          for d in artifacts/directaudio-*; do
            n=$(basename "$d")
            (cd "$d" && zip -qr "$GITHUB_WORKSPACE/release/${n}.zip" .)
          done
          ls -la release
      - name: Attach to release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*
          body: |
            **DirectAudio hot-swap driver** — native Wine → Android AAudio (`winedirectaudio.drv`).
            Two Wine ABIs × two page sizes. Each zip is the **complete 3-file driver set**,
            laid out to drop straight onto the device Proton layer's `lib/wine/`:
            - `aarch64-windows/winedirectaudio.drv` — arm64ec PE, loaded for **64-bit** guest games
            - `i386-windows/winedirectaudio.drv` — 32-bit PE, loaded for **32-bit** guest games
            - `aarch64-unix/winedirectaudio.so` — bionic unixlib (the AAudio backend)

            Which PE loads is decided by the **guest game's bitness**, not the device — a
            complete set carries both so either game type gets audio. Match the ABI to the
            layer's Wine version and the page size (sdk28 = 4KB, sdk35 = 16KB) to the device.

            **Wine 11.0** (`ge-proton11-5-bionic-staging`) — overlay onto Wine-11 layers:
            - `directaudio-wine11-arm64ec-sdk28.zip` — 4KB-page devices
            - `directaudio-wine11-arm64ec-sdk35.zip` — 16KB-page devices

            **Wine 10.0** (`proton_10.0`, the tree Proton 10.0-4 shipped from) — overlay onto Wine-10.x layers:
            - `directaudio-wine10-arm64ec-sdk28.zip` — 4KB-page devices
            - `directaudio-wine10-arm64ec-sdk35.zip` — 16KB-page devices
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
